/**
 * Domain: Arrow Configuration
 * Pure functions for arrow rendering configuration
 */

export interface ArrowStyle {
  color: string;
  thickness: number;
  opacity: number;
}

export interface ArrowColors {
  best: string;
  second: string;
  other: string;
}
